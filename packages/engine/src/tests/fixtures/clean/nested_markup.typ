*Bold with _italic inside_ and `code`.*

_Italic with *bold*._

#strong[Strong with #emph[emph] and a #link("https://example.com")[link].]

- List with *bold* and a table inside:
  #table(columns: 2, [a], [b])
