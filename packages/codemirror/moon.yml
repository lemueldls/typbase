language: "typescript"
stack: "frontend"
layer: "library"
dependsOn:
  - "engine"

tasks:
  build:
    command: "pnpm build"
    deps:
      - "engine:build"
    inputs:
      - "src/**/*"
      - "tsconfig.json"
      - "package.json"
      - "/pnpm-lock.yaml"
      - "project://engine"
    outputs:
      - "dist/**/*"

  build-dev:
    command: "pnpm build-dev"
    options:
      runInCI: false
      persistent: true
    deps:
      - "engine:build-dev"
    inputs:
      - "src/**/*"
      - "tsconfig.json"
      - "package.json"
      - "/pnpm-lock.yaml"
      - "project://engine"
    outputs:
      - "dist/**/*"
