language: "typescript"
stack: "frontend"
layer: "library"
dependsOn:
  - "typing"

tasks:
  build:
    command: "pnpm build"
    deps:
      - "typing:build"
    inputs:
      - "src/**/*"
      - "tsdown.config.ts"
      - "tsconfig.json"
      - "package.json"
      - "/pnpm-lock.yaml"
      - "project://typing"
    outputs:
      - "dist/**/*"

  build-dev:
    command: "pnpm build-dev"
    options:
      runInCI: false
      persistent: true
    deps:
      - "typing:build"
    inputs:
      - "src/**/*"
      - "tsdown.config.ts"
      - "tsconfig.json"
      - "package.json"
      - "/pnpm-lock.yaml"
      - "project://typing"
    outputs:
      - "dist/**/*"
