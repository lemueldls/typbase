Someone said:

> A quote with *markup*.
> Second line of the quote.

A footnote reference#footnote[The footnote body with `code`.].

A second footnote#footnote[A longer footnote body that wraps across lines when rendered.].
