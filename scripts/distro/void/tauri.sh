#
# Dummy build style for Tauri apps. The template's do_build/do_install do the
# work; this only gives xbps-src a known build style name to hook into.
#

do_build() {
    :
}

do_check() {
    :
}

do_install() {
    :
}
