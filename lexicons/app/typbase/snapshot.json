{
  "lexicon": 1,
  "id": "app.typbase.snapshot",
  "defs": {
    "main": {
      "type": "record",
      "description": "Full Loro snapshot of a document, written when a page's update backlog crosses the compaction threshold. Superseded update records are deleted.",
      "key": "any",
      "record": {
        "type": "object",
        "required": ["docId", "snapshot", "version"],
        "properties": {
          "docId": {
            "type": "string",
            "description": "Loro document id (workspace id or page id)."
          },
          "snapshot": {
            "type": "string",
            "description": "Base64-encoded Loro snapshot bytes."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          },
          "version": {
            "type": "string",
            "description": "Version vector JSON of the snapshot, so receivers can skip covered updates."
          }
        }
      }
    }
  }
}
