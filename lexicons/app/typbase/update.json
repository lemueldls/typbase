{
  "lexicon": 1,
  "id": "app.typbase.update",
  "defs": {
    "main": {
      "type": "record",
      "description": "One exported Loro update for one document. rkey is the Loro version vector encoded as a TID-safe base32 string, so concurrent writers get unique rkeys.",
      "key": "any",
      "record": {
        "type": "object",
        "required": ["docId", "update", "version"],
        "properties": {
          "docId": {
            "type": "string",
            "description": "Loro document id (workspace id or page id)."
          },
          "update": {
            "type": "string",
            "description": "Base64-encoded Loro update bytes."
          },
          "version": {
            "type": "string",
            "description": "Version vector JSON of the exported update, used for merge buffering."
          },
          "createdAt": {
            "type": "string",
            "format": "datetime"
          }
        }
      }
    }
  }
}
