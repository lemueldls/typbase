{
  "id": "local:calendar",
  "name": "Calendar",
  "version": "0.2.0",
  "description": "Month calendar over an events collection, with daily-note links.",
  "api": "typbase.host.v2",
  "entry": "main.typ",
  "icon": "calendar_month",
  "capabilities": ["pages.read", "pages.write", "daily.write", "plugin.data"],
  "collections": {
    "events": {
      "fields": {
        "title": { "type": "string" },
        "date": { "type": "string" },
        "time": { "type": "string", "optional": true }
      }
    }
  },
  "surfaces": [
    {
      "kind": "widget",
      "fn": "widget",
      "title": "Calendar",
      "icon": "calendar_month"
    },
    { "kind": "pane", "fn": "pane", "title": "Calendar", "icon": "calendar_month" }
  ]
}
